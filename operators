Q.10) print 1 for true and 0 for false .

a) if its sunday and its snowing --> true 
#include <stdio.h>

int main() {
    int isSunday = 1;
    int isSnowing = 1;
    printf("%d \n" , isSunday && isSnowing);

    return 0;


b) if its monday and its raining --> true 
#include <stdio.h>

int main() {
    int ismonday = 0;
    int israining = 1;
    printf("%d \n" , ismonday || israining);

    return 0;
}

c) if a number is greater than 9 and less than 100 --> true 
( 2 digit number)
#include <stdio.h>

int main() {
    int x;
    printf("enter number : ");
    scanf("%d" , &x);
    
    printf("%d \n" , x>9 && x<100);
    return 0;
}
