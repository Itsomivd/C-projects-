Q.5)  take a number (n) from user and output its cube (n*n*n)

ans:- // to fine cube of number n
#include <stdio.h>
int main() {
    int n;
    printf("enter n");
    scanf("%d", &n);
    
    printf("cube is : %d", n * n * n);
    return 0;
}

explination:-

//               -for a statement
int             -to define a variable 
scanf        -to take input
%d            -defines that side is integer
&               -shows the location of variable 
